import React from "react";
import OTPCheck from "./OTPCheck";

describe("Check the OTP has matched or not", () => {
  test.todo("Display a success message if otp match");
  test.todo("Display error if otp does not match");
  test.todo("Check the next button to be disabled when OTP not entered");
  test.todo("Check when user inputs correct OTP the button gets enabled");
});
