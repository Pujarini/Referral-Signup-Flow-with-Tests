import React from "react";
import Referral from "./Referral";

describe("Check the referral", () => {
  test.todo("Check the referral box and a checkbox is present");
  test.todo("User should input alphanumeric referral else give an error message");
  test.todo("On valid referral code given by user the button should be enabled which was disabled by default");
  test.todo("In case user user does not have referral code then he checks the check that says I don'\t have referral code");
});